[user]
	name = Mark Steve Samson
	email = hello@marksteve.com
	signingKey = C4E11074DACBC623602E2FA126675B54DFC1C9A1
[push]
	default = simple
	autoSetupRemote = true
[log]
	decorate = short
[alias]
	pull = pull --ff-only
	l = log --oneline --graph
	next = stack next
	prev = stack previous
	reword = stack reword
	amend = stack amend
	sync = stack sync
	run = stack run
[credential]
	helper = cache
[core]
	excludesFile = ~/.gitignore
[merge]
	tool = smerge
[diff]
	tool = icdiff
[difftool]
	prompt = false
[difftool "icdiff"]
	cmd = icdiff --line-numbers $LOCAL $REMOTE
[filter "media"]
	required = true
	clean = git media clean %f
	smudge = git media smudge %f
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
	process = git-lfs filter-process
[mergetool "smerge"]
	cmd = smerge mergetool \"$BASE\" \"$LOCAL\" \"$REMOTE\" -o \"$MERGED\"
	trustExitCode = true
[includeIf "gitdir:~/Dev/toggl/"]
	path = ~/.gitconfig-toggl
[init]
	defaultBranch = main
[pull]
	ff = only
