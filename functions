isstart()
{
  workon isyncd-server
  supervisord > /dev/null 2>&1 &
  sleep 5
  supervisorctl
}

test_js()
{
  jt test -- -s -v tests.test_jasmine_fixtures
  pushd spec
  python -m SimpleHTTPServer
  popd
}
